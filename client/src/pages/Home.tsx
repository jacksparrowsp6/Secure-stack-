import { Link, useLocation } from "wouter";
import { ArrowRight, ShieldCheck } from "lucide-react";
import { articles } from "@/lib/articles";
import { SiteLayout, articlePath } from "@/components/SiteLayout";
import { setSeo } from "@/lib/seo";
import { useEffect } from "react";

export default function Home() { const [location] = useLocation(); const category = location.startsWith("/category/"); const visible = category ? articles.filter((a) => a.category === "VPN Troubleshooting") : articles; useEffect(() => setSeo({ title: category ? "VPN Troubleshooting Guides | SecureStack Hub" : "VPN Troubleshooting & Privacy Guides | SecureStack Hub", description: "Straightforward VPN troubleshooting and online privacy guides with clear steps, practical limits, and no hype.", path: category ? "/category/vpn-troubleshooting/" : "/" }), [category]); return <SiteLayout><main><section className="simple-hero"><div className="container"><p className="eyebrow"><ShieldCheck size={14} /> Practical security guidance</p><h1>{category ? "VPN troubleshooting guides" : "Clear answers when your connection gets complicated."}</h1><p>{category ? "Work through the most common VPN, Wi-Fi, DNS, and local-network problems." : "Straightforward guides for VPN problems, device setup, and safer browsing—written to be useful, not intimidating."}</p></div></section><section className="container article-index"><div className="section-heading"><div><p className="eyebrow">{category ? "All guides" : "Start here"}</p><h2>{category ? "VPN fixes, in plain language" : "Latest practical guides"}</h2></div></div><div className="article-list">{visible.map((a) => <Link href={articlePath(a.slug)} className="list-row" key={a.slug}><div><span className="card-category">{a.category}</span><h3>{a.title}</h3><p>{a.description}</p></div><ArrowRight size={17} /></Link>)}</div></section></main></SiteLayout>; }
